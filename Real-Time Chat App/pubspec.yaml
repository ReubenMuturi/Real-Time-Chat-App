dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^3.0.0  # Trending state management
  firebase_core: ^3.0.0
  firebase_auth: ^5.0.0
  cloud_firestore: ^5.0.0
  cached_network_image: ^3.3.1  # For avatars
  intl: ^0.19.0  # Date formatting

dev_dependencies:
  flutter_test:
    sdk: flutter